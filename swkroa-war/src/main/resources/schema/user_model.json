{
  "description": "Representation of a User.",
  "type": "object",
  "extends": {
    "$ref": "person_model.json"
  },
  "properties": {
    "userUID": {
      "description": "Uniquely identifies the User.",
      "type": "long",
      "required": true
    },
    "username": {
      "description": "A unique, human readable, identifier for the User.",
      "type": "string",
      "required": true
    }
  },
  "additionalProperties": false
}