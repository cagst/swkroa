<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:sec="http://www.springframework.org/schema/security"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
                           http://www.springframework.org/schema/beans/spring-beans.xsd
                           http://www.springframework.org/schema/security
                           http://www.springframework.org/schema/security/spring-security.xsd">

  <sec:global-method-security pre-post-annotations="enabled"/>

  <bean id="passwordEncoder" class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder">
    <constructor-arg value="12"/>
  </bean>

  <bean id="successHandler" class="com.cagst.swkroa.security.SigninSuccessHandler">
    <constructor-arg ref="userService"/>
  </bean>

  <bean id="failureListener" class="com.cagst.swkroa.security.AuthenticationFailureListener">
    <constructor-arg ref="userService"/>
  </bean>

  <bean id="changePasswordFilter" class="com.cagst.swkroa.security.ForceChangePasswordFilter">
    <property name="changePasswordUrl" value="/auth/changepwd"/>
  </bean>

  <!-- Non Secured Resources -->
  <sec:http pattern="/static/**" security="none"/>

  <!-- Secured Resources -->
  <sec:http auto-config="true" use-expressions="true" access-denied-page="/auth/notauthorized">
    <!-- Restrict access to ALL other pages -->
    <sec:intercept-url pattern="/public/**" access="permitAll"/>
    <sec:intercept-url pattern="/auth/**" access="permitAll"/>
    <sec:intercept-url pattern="/**" access="isAuthenticated()"/>

    <!-- Set the login page and what to do if login fails -->
    <sec:form-login login-page="/auth/signin"
                    authentication-failure-url="/auth/signin"
                    authentication-success-handler-ref="successHandler"/>

    <sec:logout invalidate-session="true"
                delete-cookies="JSESSIONID"
                logout-success-url="/auth/signedout"/>

    <sec:custom-filter ref="changePasswordFilter" position="LAST"/>

    <!--
        <sec:session-management invalid-session-url="/auth/timedout" />
    -->
  </sec:http>

  <sec:authentication-manager alias="authenticationManager">
    <sec:authentication-provider user-service-ref="userService">
      <sec:password-encoder ref="passwordEncoder"/>
    </sec:authentication-provider>
  </sec:authentication-manager>

</beans>
